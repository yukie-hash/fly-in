### 要約
- ドローンを最も少ない手数でゴールさせる

### 入力
- ドローン
  - 数

- ゾーン
  - `start_hub: <name> <x> <y> [metadata]` は開始ゾーンを示す。
  - `end_hub: <name> <x> <y> [metadata]` は終了ゾーンを示す。
  - `hub: <name> <x> <y> [metadata]` は通常のゾーンを定義する。
- すべてのメタデータは任意で、角括弧 `[...]` 内に記述し、デフォルト値を持つ：
  - `zone=<type>`（デフォルト：normal）
  - `color=<value>`（デフォルト：none）
  - `max_drones=<number>`（デフォルト：1）— そのゾーンに同時に滞在できる最大ドローン数
  - 角括弧内のタグは任意の順序で記述できる。
- ゾーンの種類  
  - **normal** — 標準ゾーン。移動コストは1ターン（デフォルト）
  - **blocked** — 進入不可ゾーン。ドローンはこのゾーンに入ったり通過したりしてはならない。これを使う経路は無効となる。
  - **restricted** — 敏感または危険なゾーン。このゾーンへの移動には2ターンかかる。
  - **priority** — 優先されるゾーン。このゾーンへの移動コストは1ターンだが、経路探索において優先されるべきである。
  
- 接続 `connection: <name1>-<name2> [metadata]` で定義される：
  - 2つのゾーン間の双方向接続（エッジ）を定義する。
  - connection の構文ではゾーン名にハイフンを使うことは禁止されている。
  - 任意のメタデータを角括弧 `[...]` で指定できる：
    - `max_link_capacity=<number>`（デフォルト：1）— この接続を同時に通過できる最大ドローン数
- コメントは `#` で始まり、無視される。


### 出力形式
- ```ドローンのid、ゾーンの名前```
- もしくは、```ドローンid、接続部の名前```

### 制約
- ゾーンのターン数を正しく集計する  
  
複数ドローンで
- ゾーン、接続の最大ドローン数を考慮する
- ドローン同士の衝突を回避する(待機する)

### 必要なクラス
- Zone
  - type
  - color
  - max_drornes
- Connection
  - 
- Drorne
  - id
  - current_zone

- Graph?
  - zones　　　　辞書
  - conections　リスト
  - start-hub
  - end-hub

- Parser
- Simulator
 - Drorneの動き
- Visualizer


### 雑な設計
パースする、アルゴリズムでゴールさせる、図にする

paserが入力ファイルをzornとconnectionからなるgraphに変換  
→ simulatorがgraphとdrorneを使ってシミュレーションを実行  
→ Visualizerが結果を出力する

### メモ
- fileの読み込みはどこから？
- connection部分の台数制限を実装する
- restricted ゾーンへ飛行中のドローンの場合は D<ID>-<connection> の形式に従わなければならない。の実装
- ドローンの待機列が在る場合にどこまで柔軟に対処するか実装する