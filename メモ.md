### 要約
- ドローンを最も少ない手数でゴールさせる

### 入力
- ドローン
  - 数

- ゾーン
  - `start_hub: <name> <x> <y> [metadata]` は開始ゾーンを示す。
  - `end_hub: <name> <x> <y> [metadata]` は終了ゾーンを示す。
  - `hub: <name> <x> <y> [metadata]` は通常のゾーンを定義する。
- すべてのメタデータは任意で、角括弧 `[...]` 内に記述し、デフォルト値を持つ：
  - `zone=<type>`（デフォルト：normal）
  - `color=<value>`（デフォルト：none）
  - `max_drones=<number>`（デフォルト：1）— そのゾーンに同時に滞在できる最大ドローン数
  - 角括弧内のタグは任意の順序で記述できる。
- ゾーンの種類  
  - **normal** — 標準ゾーン。移動コストは1ターン（デフォルト）
  - **blocked** — 進入不可ゾーン。ドローンはこのゾーンに入ったり通過したりしてはならない。これを使う経路は無効となる。
  - **restricted** — 敏感または危険なゾーン。このゾーンへの移動には2ターンかかる。
  - **priority** — 優先されるゾーン。このゾーンへの移動コストは1ターンだが、経路探索において優先されるべきである。
  
- 接続 `connection: <name1>-<name2> [metadata]` で定義される：
  - 2つのゾーン間の双方向接続（エッジ）を定義する。
  - connection の構文ではゾーン名にハイフンを使うことは禁止されている。
  - 任意のメタデータを角括弧 `[...]` で指定できる：
    - `max_link_capacity=<number>`（デフォルト：1）— この接続を同時に通過できる最大ドローン数
- コメントは `#` で始まり、無視される。


### 出力形式
- ```ドローンのid、ゾーンの名前```
- もしくは、```ドローンid、接続部の名前```

### 制約
- ゾーンのターン数を正しく集計する  
  
複数ドローンで
- ゾーン、接続の最大ドローン数を考慮する
- ドローン同士の衝突を回避する(待機する)

### 必要なクラス
- Zone
  - type
  - color
  - max_drornes
- Connection
  - 
- Drorne
  - id
  - current_zone

- Graph?
  - zones　　　　辞書
  - conections　リスト
  - start-hub
  - end-hub

- Parser
- Simulator
 - Drorneの動き
- Visualizer


### 雑な設計
パースする、アルゴリズムでゴールさせる、図にする

paserが入力ファイルをzornとconnectionからなるgraphに変換  
→ simulatorがgraphとdrorneを使ってシミュレーションを実行  
→ Visualizerが結果を出力する

### メモ
- ネットワークとドローンの位置を表示するグラフィカルインターフェース
    ・各ゾーンを3×3の枠で描き、中央に現在のドローン数 / 最大収容数（▲2/2）を表示。
    　枠の上にゾーン名を表示すし、ゾーン名の下にカッコで囲んでゾーンタイプを表示する。
    ・start/goalは二重罫線で表示。通常のhubは実線で表示。立入禁止区域は、破線で表示。優先区域は丸角線、restrictedゾーンは太線で表示。
    ・通路は実線、容量制限のあるリンクは二重線で、上に(cap:2)のように表示させる。長さは８文字分。
    ・斜めのリンクはL字で表す。
    ・ドローンの数に応じて▲が増減するようにして。6機以上の場合も枠からはみ出さないよう、▲▲▲▲▲… 8/∞ のように省略記号と実数を併記。０台の時には▲を表示しない。
- ドローンの待機列ができる場合にどこまで柔軟に対処するか実装する
- パーサーのエッジケース
- エラー処理
- maxhrizon
- ライブコーディング対策

Day7
├── Commit1
│   ReservationTableクラス
│
├── Commit2
│   PathFinderクラス
│
├── Commit3
│   __main__を書き換え
│
├── Commit4
│   simulate接続
│
└── Commit5
    動作確認・リファクタリング

Step 1
ReservationTableだけ追加
既存コードは変更しない

Step 2
TimeAwarePathFinderを追加
D1だけで探索結果を確認

Step 3
mainを変更
D1→予約→D2→予約…を実装

Step 4
新しいTimedPathをsimulateで実行

Step 5
CooperativePlanner化・ファイル分割・命名整理

### PathFinder
```
START
  ↓
heapに(start, 0)を入れる
  ↓
heapから最も良い候補を取り出す
  ↓
現在の(Zone, Turn)を調べる
  ↓
ゴール？
├─ YES → previousから経路復元 → 終了
│
└─ NO
    ↓
    2種類の次の状態を作る

    ┌────────────┐
    │            │
   待機          移動
    │            │
    |      隣接ゾーンを順番に調べる
(A, t+1)   (B, t+cost)
    │            │
予約OK？       Zone予約OK？
    |         Connection予約OK？
    |         　　|
    |         previousにどこから来たか記録
    │            │
    └─────┬──────┘
          ↓
       heapへ追加
          ↓
       また最初へ
```

### Makefile
- make install：flake8、mypy をインストール
- make run：デフォルトのeasyマップを実行
- make debug：pdb で実行
- make clean：Python・mypyキャッシュを削除
- make lint：指定された全フラグで検査
- make lint-strict：mypy --strict で検査
対象マップは次のように変更できます。
```
make run MAP=01_the_impossible_dream.txt
make debug MAP=01_the_impossibl
```